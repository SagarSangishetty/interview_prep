Select avg (salary) from table
---------------
Duplicates values fetch

Select name,email_id from table group by name having count (*) > 1 --- incorrect

correct 

SELECT name
FROM table
GROUP BY name
HAVING COUNT(*) > 1;

✅ Case 2: Find duplicate (name, email_id) pairs
SELECT name, email_id
FROM table
GROUP BY name, email_id
HAVING COUNT(*) > 1;


----------------------------------------------------------------------------------
Second highest salary

Select max(salary) from employee where salary IN (Select salary from employee where salary < max(salary)) -- incorrect

correct 

SELECT MAX(salary)
FROM employee
WHERE salary < (
    SELECT MAX(salary)
    FROM employee
);


--------------------------------------
Join 2 tables 

Select A.coloum,B.coloum from table 1 as A inner join table 2 as B
where A.coloum=B.coloum and 
condition 

Correct command

SELECT A.emp_id, B.emp_name
FROM employees A
INNER JOIN employee_details B
ON A.emp_id = B.emp_id
WHERE A.status = 'ACTIVE';

-------------------------------------------------

find . -type f -name abc* 

to redirect output  find . -type f -name abc* 1> output.log 2> error.log

to ignore error find . -type f -name abc* | grep -iv "PERMISSION"

----------------------------------------------------


1000 line in file

awk -F ',' '{print $84}'

sed -i 's/sagar/kedar/g' -- replace globally
sed -i 's/sagar/kedar/'  -- first word only

✅ Replace a word only on line 84

sed '84s/OLD_WORD/NEW_WORD/' file.txt  --- Replaces the first occurrence only on line 84
sed '84s/OLD_WORD/NEW_WORD/g' file.txt   -- Replaces all occurrences on line 84


-------------------------------------------------
➡ Prints the matched line and 3 lines after it
grep -A 3 "matching word" file_name.txt

➡ Prints the matched line and 3 lines before it
grep -B 3 "matching word" file_name.txt

3️⃣ Print before and after
grep -C 2 "ERROR" application.log





